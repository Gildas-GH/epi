<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
    <channel>
        <title>{{ items.0.snippet.channelTitle }}</title>
        <link>http://www.YourSite.com</link>
        <description>{{ channel_data.items.0.snippet.description }}</description>
        <language>en-us</language>

        <image>
            <url>{{ channel_data.items.0.snippet.thumbnails.maxrhes.url }}</url>
        </image>
        
        {% for item in items %}
        <item>
            <title>{{ item.snippet.title }}</title>
            <description>{{ item.snippet.description }}</description>
            <enclosure url="http://{{ request.get_host }}{% url 'download' item.snippet.resourceId.videoId %}" type="audio/m4a"/>
            <guid>{{ item.snippet.resourceId.videoId }}</guid>
            <pubDate>{{ item.snippet.publishedAt }}</pubDate>
        </item>
        {% endfor %}
    </channel>
</rss>