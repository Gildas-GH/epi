<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
    <channel>
        <title>{{ items.0.snippet.channelTitle }}</title>
        <link>http://www.YourSite.com</link>
        <description>{{ channel_data.items.0.snippet.description }}</description>
        <language>en-us</language>

        <image>
            <url>{{ channel_data.items.0.snippet.thumbnails.high.url }}</url>
        </image>
        
        {% for item in items %}
        <item>
            <title>{{ item.snippet.title }}</title>
            <description>{{ item.snippet.description }}</description>
            <enclosure url="http://{{ request.get_host }}{% url 'download' podcast_type item.snippet.resourceId.videoId %}.{{ media_extension }}" type="audio/mp4" length='500'/>
            <guid>http://{{ request.get_host }}{% url 'download' podcast_type item.snippet.resourceId.videoId %}.{{ media_extension }}</guid>
            <pubDate>{{ item.snippet.publishedAt }}</pubDate>
        </item>
        {% endfor %}
    </channel>
</rss>